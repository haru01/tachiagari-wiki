# 現在アクティブな案件（個人案件）の slug を指定する。各自ローカル設定（.env は gitignore）。
# 使い方: cp .env.example .env して値を編集する。未設定・.env 無しなら self にフォールバックする。
# PREFIX（接頭辞）は projects/<slug>/wiki/ の既存レコードID／slug の大文字から自動導出される。
CURRENT_PROJECT=self
